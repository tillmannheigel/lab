// Optional: hard-code a backend URL for the deployed Pages site.
// Leave empty to let users configure it via the UI / ?api= query / localStorage.
// Example:
//   window.YTT_API_BASE = "https://your-backend.fly.dev";
window.YTT_API_BASE = "";
